# Large model checkpoints (mount at runtime)
checkpoint/

# Output data
data/output/

# Python cache
__pycache__/
*.pyc
*.pyo

# Backup files
*.backup
*.bak
*_bak*

# Docker files
# NOTE: docker/ must stay in the build context: docker/Dockerfile COPYs
# docker/requirements.txt and docker/entrypoint.sh. Excluding it breaks the
# documented `docker build -f docker/Dockerfile -t eva:latest .` command.
Dockerfile
docker/Dockerfile_bak*
docker/EVA.def
docker/smoke_test.sh
.dockerignore

# Figures are not part of the image (34 MB of build context)
fig/

# IDE / OS
.git/
.gitignore
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Notebooks
notebooks/
!notebooks/
notebooks/*
!notebooks/interpretability_analysis/
notebooks/interpretability_analysis/*
!notebooks/interpretability_analysis/sae_repro_release/
!notebooks/interpretability_analysis/sae_repro_release/**

# Docs / reference
docs/
reference/

# Finetune scripts
finetune/
